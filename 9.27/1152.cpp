#include<bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL), cout.tie(NULL);
    string s;
    int count = 0;
    while(cin >> s){
        count++;
    }
    cout << count << '\n';
    return 0;
}